@model IEnumerable<User>

<h2>List of Users</h2>
<table class="table">
  @foreach (var user in Model)
  {
    <tr>
      <td>@user.Email</td>
      <td>
        <a class="btn btn-sm btn-primary" asp-action="EditRole" asp-route-userid="@user.Id">Rights</a>
      </td>
    </tr>
  }
</table>